{
	% recurse-func.488: check correctness of recursive function call
	% Author: KyoKeun Park

	function foo ( i : integer ) : integer
	{
		if ( i = 5 ) then
			return with 5
		else
			return with foo ( i + 1 )
	}

	write "Result: ", foo(0), newline
}
