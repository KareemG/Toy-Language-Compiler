
{
    procedure print(b: boolean)
    {
        if b then write "hello"
        write " world"
        return
    }

    function add(a: integer, b: integer) : integer
    {
        return with a + b
    }

    var array[2], c: integer
    read array[0]
    read array[1]

    if a < b then
        c := add(array[0], array[1])
    else
        if b < c then
            c := add(array[1], array[0])
        else
            c := array[0]

    repeat
    {
        c := c - 1

        if c = 69 then exit
    }
    until c = 0
}
