{
    function a : boolean
    {
        write "evaluate a", newline
        return with true
    }

    function b : boolean
    {
        write "evaluate b", newline
        return with false
    }

    if a or b then
        write "pass", newline

    if not a and b then
        write "pass", newline

    if not a or not b then
        write "pass", newline
}
